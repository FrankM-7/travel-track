import React from 'react';

const Arc = ({ startLat, startLng, endLat, endLng, color }) => {
  return {
    startLat,
    startLng,
    endLat,
    endLng,
    color, // Arc color
  };
};

export default Arc;
